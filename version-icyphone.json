{
  "version":"0.3.3 beta",
  "notes": [
    "Added some new logging to debugMode for support help",
    "Added enable, disable & isDisabled exports",
    "Added createNotification export",
    "Added callNumber export",
    "Fixed some config to automatically detect if an older version of ESX is being used"
  ]
}
